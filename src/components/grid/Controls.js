import React from 'react';

const Controls = () => {
  return (
    <div>
      <button>Play</button>
      <button>Pause</button>
      <button>Reset</button>
    </div>
  );
};

export default Controls;
